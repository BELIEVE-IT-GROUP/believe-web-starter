'use strict';

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var index = require('../store/index.cjs');

const BaseIcon = react.forwardRef(
  (props, ref) => {
    const {
      width,
      height,
      // custom
      size,
      // rest
      ...otherProps
    } = props;
    const theme = index.getStore();
    const mergedProps = react.useMemo(() => {
      const _props = { ...otherProps };
      for (const _key in theme) {
        const key = _key;
        if (key === "size") continue;
        _props[key] = _props[key] ?? theme[key];
      }
      return _props;
    }, [otherProps, theme]);
    return /* @__PURE__ */ jsxRuntime.jsx(
      "svg",
      {
        ref,
        width: width ?? size ?? theme.width ?? theme.size,
        height: height ?? size ?? theme.height ?? theme.size,
        ...mergedProps
      }
    );
  }
);

exports.BaseIcon = BaseIcon;
//# sourceMappingURL=base-icon.cjs.map
