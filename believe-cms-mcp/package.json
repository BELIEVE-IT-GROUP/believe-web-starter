{
  "name": "believe-cms-mcp",
  "version": "0.1.0",
  "description": "MCP server (the hands) for the Believe Web Factory: writes tenants, pages and blocks into the Payload CMS and provisions Coolify landings.",
  "type": "module",
  "bin": {
    "believe-cms-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "src/flowbite-catalog.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
