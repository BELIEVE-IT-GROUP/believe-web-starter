{
  "name": "puck-cms-mcp",
  "version": "0.1.0",
  "description": "MCP server (the hands) for the Believe Puck CMS: lists/creates tenants, manages tokens, publishes page content and triggers deploys. Híbrido: tenants/deploy locales (repo + ssh), contenido remoto vía endpoint API-key.",
  "type": "module",
  "bin": {
    "puck-cms-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "smoke": "node smoke.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
